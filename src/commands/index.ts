import * as ping from './ping';
import * as channels from './channels';
import * as join from './voice-channel/join';
import * as leave from './voice-channel/leave';

export const commands = {
  ping,
  channels,
  join,
  leave,
};
